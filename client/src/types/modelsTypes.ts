export interface BaseModelTypes {
  prompt: string;
  negative_prompt: string;
  steps: number;
  batch_size: number;
  cfg_scale: number;
  width: number;
  height: number;
  image: string;
  auto: boolean;
}
